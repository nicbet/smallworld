# Walkthrough (summary)

Ch04 comparison retrofit (audit items F1/F2/F3/F5 plus optional F4). Unity added to the pipeline comparison; the extrapolation callout now names Unity as the only major engine shipping it (Rigidbody.interpolation Extrapolate, with its documented artifact) and explains the engine-wide policy choice; the fixed-timestep survey was rewritten to state honestly that availability is universal but defaults diverge (Unreal's variable-delta tick, semi-fixed substepping); device loss now commits to the recovery walk as the designed end-point per the no-roadmap rule; a feedback-channel precedent note covers Unity FrameTimingManager and UE stat telemetry. Roadmap sweep removed v1/v2 comments and "future refinement". The sw-c63a81 fact-check also corrected the opening war story on the main-branch side (Dark Souls II) and DRS examples. All claims vendor-doc verified; details in the diff.