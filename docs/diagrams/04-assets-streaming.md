# Assets, Pipeline, Streaming, Serialization

The runtime asset layer, the offline cook pipeline over the VFS, the staging upload path, the
two-layer streaming system, and the shared serialization registry. See: Assets & Resources,
Resource Pipeline & Filesystem, Streaming, Staging Pool, OQ 5/17/20/21/27.

## Assets & the Upload Path

@import "04-assets.mmd" {as="mermaid"}

## Two-Layer Streaming

@import "04-streaming.mmd" {as="mermaid"}

## Serialization (one registry, three consumers)

@import "04-serialization.mmd" {as="mermaid"}
