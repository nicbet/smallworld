\begin{table}[!tb]
\centering
\small
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{@{}p{0.22\textwidth}p{0.35\textwidth}p{0.35\textwidth}@{}}
\toprule
\textbf{Context} & \textbf{Owns (persistent state)} & \textbf{Communicates via} \\
\midrule
Game Thread         & World, input, time, systems, asset server
                    & Render description $\rightarrow$ Render Thread; feedback $\leftarrow$ Render Thread \\[3pt]
Render Thread       & GPU context, retained render scene, resource pools, render graph
                    & Descriptions in; feedback out \\[3pt]
Game Worker Pool    & Nothing persistent; borrows work
                    & Scoped tasks: \texttt{join()}, \texttt{parallel\_for()} \\[3pt]
Render Worker Pool  & Nothing persistent; borrows work
                    & Scoped tasks: \texttt{join()}, \texttt{parallel\_for()} \\[3pt]
Streaming Coord.    & Demand queue, budget arbiter
                    & Demand $\leftarrow$ Game; \texttt{UploadBatch} $\rightarrow$ Render \\[3pt]
Audio Thread        & Mixer, voices, output stream
                    & \texttt{AudioCommands} drained per frame \\[3pt]
IO Pool             & Nothing persistent
                    & Tasks from asset server and streaming \\
\bottomrule
\end{tabular}
\caption{Thread ownership map. Each named context owns its persistent mutable state; worker pools borrow bounded work rather than accumulating state of their own.}
\label{tab:thread-ownership}
\end{table}
\FloatBarrier
